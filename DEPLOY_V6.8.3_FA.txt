NOVA TRADER V6.8.3 — FINAL CONNECTION FIX

ریشه خطا:
در V6.8.2 داخل تابع metered_cost_status متغیر محلی با نام b تعریف شده بود و همان نام تابع تنظیمات b() را shadow می‌کرد. در نتیجه /api/dashboard بعد از احراز هویت با TypeError می‌ترکید. چون پاسخ 500 در مرورگر cross-origin می‌توانست بدون CORS قابل‌خواندن باشد، Safari فقط Load failed نشان می‌داد.

اصلاحات V6.8.3:
- رفع shadow شدن b در metered_cost_status و _cost_block
- CORS کامل برای GitHub Pages / Northflank
- preflight OPTIONS با X-NOVA-Key
- نرمال‌سازی NOVA_ADMIN_KEY در سرور و مرورگر (حذف فاصله و quote ناخواسته)
- endpoint جدید /api/auth-check
- محافظت صریح /api/dashboard با X-NOVA-Key
- خطاهای واضح 401 / 503 / CORS / NETWORK / TIMEOUT در Dashboard
- حذف Content-Type غیرضروری از GET
- Health metadata: admin_key_configured و cors_enabled

فایل‌های لازم:
1) app.py -> Backend روی Northflank
2) requirements.txt -> Backend dependencies
3) index.html -> GitHub Pages dashboard

Northflank:
Build command:
pip install -r requirements.txt

Start command:
uvicorn app:app --host 0.0.0.0 --port $PORT

Health check:
Path: /health
Protocol: HTTP
Port: همان service port تعریف‌شده در Northflank

Environment variable ضروری:
NOVA_ADMIN_KEY=<YOUR_PRIVATE_ADMIN_KEY>

نکته: بهتر است مقدار NOVA_ADMIN_KEY را بدون کوتیشن وارد کنی. V6.8.3 حتی اگر quote یا space اضافه باشد آن را normalize می‌کند، اما مقدار ساده‌تر بهتر است.

ترتیب Deploy:
1) app.py و requirements.txt را در repo/backend جایگزین کن.
2) Northflank را Redeploy کن.
3) در Safari باز کن:
   https://p01--nova-trader-cloud-v2--snkzwswjqkdc.code.run/health
   باید version = 6.8.3 و ok = true ببینی.
4) index.html جدید را روی GitHub Pages جایگزین کن.
5) صفحه GitHub Pages را یک بار Hard Refresh / Reload کن.
6) Northflank URL را وارد کن:
   https://p01--nova-trader-cloud-v2--snkzwswjqkdc.code.run
7) همان NOVA_ADMIN_KEY را وارد کن و CONNECT / SAVE بزن.
8) در حالت صحیح باید نمایش داده شود:
   CONNECTED
   Backend 6.8.3 • AUTH OK • UP

تست‌های انجام‌شده روی نسخه نهایی:
- Python compile: OK
- JavaScript syntax: OK
- CORS preflight OPTIONS: 200
- /health: 200
- /api/auth-check با key درست: 200
- /api/auth-check با key غلط: 401
- /api/dashboard با key درست: 200
